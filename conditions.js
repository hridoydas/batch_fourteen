// odd/even => 10 % 2 => 0 => even|| odd

// a =5 and b= "5"
function condition() {
  const data = 2;
  if (data % 2 === 0 || data === 0) {
    console.log(`${data} is a even number`);
  }
}

condition();

// 74 => A
